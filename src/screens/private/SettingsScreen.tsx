import { Center } from 'native-base'

import { Background } from '@/components'

export function SettingsScreen() {
  return (
    <Background>
      <Center flex={1}>SettingsScreen</Center>
    </Background>
  )
}
