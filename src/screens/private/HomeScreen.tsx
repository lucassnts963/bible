import { Center } from 'native-base'

import { Background } from '@/components'

export function HomeScreen() {
  return (
    <Background>
      <Center flex={1}>HomeScreen</Center>
    </Background>
  )
}
